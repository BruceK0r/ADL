# ADL 多域推荐实验说明

本文记录基于 `processed_3domains` 数据构建 Amazon 多域推荐任务、适配 ADL
模型以及两组实验结果的完整口径。实验包括：

- 三域：Beauty + Electronic + Phone；
- 两域：Electronic + Phone，Beauty 在预处理开始前即被排除。

> 本实验将交互序列转换为隐式反馈排序任务。测试指标是在“1 个正样本 +
> 100 个固定负样本”的候选集合上计算的 sampled-ranking 指标，不是全物品库
> 检索指标，也不能与 CTR/AUC 结果直接比较。

## 1. 数据预处理

### 1.1 输入数据

每个域使用以下文件：

| 文件 | 用途 |
|---|---|
| `all_item_seqs.json` | 用户交互过的物品序列 |
| `all_rating_seqs.json` | 与物品序列逐位置对应的评分 |
| `all_timestamp_seqs.json` | 与物品序列逐位置对应的时间戳 |
| `id_mapping.json` | 域内用户、物品原始 ID 与整数 ID 的映射 |
| `qwen_embeddings_1024.pt` | 按物品顺序保存的 1024 维文本向量 |
| `qwen_embeddings_1024.manifest.json` | 文本向量的数量、维度和行顺序说明 |

预处理首先检查三个序列文件的用户集合及序列长度是否一致，并检查 Qwen
向量形状是否为 `[域内物品数, 1024]`、行顺序是否与
`id_mapping.json:id2item[1:]` 一致。

当前实现把每一条已记录交互都视为正反馈。评分只用于检查序列对齐，不作为
特征，也没有按评分高低过滤交互。

### 1.2 建立统一的多域 ID 空间

1. 同一个原始用户 ID 在所选域中共享一个全局用户 ID。
2. 每个域的物品 ID 增加独立偏移量，得到互不冲突的全局物品 ID。
3. 各域 Qwen 向量按照相同的域顺序拼接，并在第 0 行加入 padding 向量。
4. 同一用户在所有所选域中的交互合并后，按照
   `(timestamp, domain_id, item_id)` 排序。

三域数据的物品空间如下：

| 域 | 物品数 | 全局 ID 偏移量 |
|---|---:|---:|
| Beauty | 91,599 | 0 |
| Electronic | 96,642 | 91,599 |
| Phone | 20,324 | 188,241 |

两域数据不是三域结果的简单子集。预处理程序先选择 Electronic、Phone，随后
重新构建用户集合、物品空间、时间分位点、历史序列、训练热门度和负采样池。
因此 Beauty 不会进入两域实验的历史或任何统计量。

### 1.3 按全局时间划分 train/validation/test

在每套实验视图中，收集所选域的全部事件时间戳，并使用全局时间分位点划分：

- 按时间排序后最早约 80% 的事件：训练集；
- 接下来约 10% 的事件：验证集；
- 最后约 10% 的事件：测试集。

实现使用 `numpy.quantile(..., method="higher")` 计算边界。时间戳等于边界的
事件归入较早的 split，因此实际事件比例可能不会严格等于 80/10/10。

| 实验 | 训练截止时间 | 验证截止时间 |
|---|---|---|
| 三域 | `1669281645804` ms（2022-11-24 09:20:45 UTC） | `1675748336508` ms（2023-02-07 05:38:56 UTC） |
| 两域 | `1668791368120` ms（2022-11-18 17:09:28 UTC） | `1675120089355` ms（2023-01-30 23:08:09 UTC） |

这是全局时间切分，不是按用户分别切分，也不是 leave-one-out。两域和三域的
事件总体不同，因此时间边界也不同。

### 1.4 从交互序列构造排序样本

对每个用户按时间依次处理事件。设当前事件为目标物品：

1. 在把目标物品加入历史前，取该用户此前最多 50 个交互作为历史；
2. 历史不足 50 时在左侧补 0；
3. 至少已有 1 个历史事件才生成样本，因此用户在所选域中的第一次交互不会
   成为训练或评估目标；
4. 根据目标时间戳决定样本属于 train、validation 还是 test；
5. 当前目标物品放在候选列表第 0 列，随后加入负物品；
6. 样本写出后才把当前目标加入历史，从而避免目标泄漏。

三域实验中的 `history` 是用户在三个域中的统一历史。例如，Phone 目标可以
使用此前的 Beauty、Electronic、Phone 事件。模型会从同一历史进一步计算：

- 全局历史表示：全部历史物品文本向量的均值；
- 同域历史表示：仅保留与当前目标域相同的历史物品后求均值。

如果此前没有同域事件，同域历史表示为零向量。两域实验的历史只可能包含
Electronic 和 Phone。

每条样本还包含 8 维时间特征：

1. `log(1 + 此前全部交互数)`；
2. `log(1 + 此前同域交互数)`；
3. `log(1 + 距上一次全局交互的天数)`；
4. `log(1 + 距上一次同域交互的天数)`；
5. 周期为 7 天的正弦、余弦；
6. 周期为 365.25 天的正弦、余弦。

最终 NPZ shard 中保存：

| 字段 | 形状或含义 |
|---|---|
| `user` | 全局用户 ID |
| `domain` | 当前目标所属域 |
| `history` | `[50]`，跨所选域的历史物品 ID |
| `time_features` | `[8]`，计数、间隔和周期特征 |
| `candidates` | 训练为 `[1 正 + 4 负]`；验证/测试为 `[1 正 + 100 负]` |

### 1.5 负样本构造

负样本严格从**目标物品所属域**中采样，而不是从所有域混合采样。具体流程为：

1. 对每个域，仅使用训练截止时间之前出现过的物品建立候选池；
2. 统计这些物品在训练期的交互次数；
3. 一半负样本均匀采样，另一半按照 `popularity^0.75` 采样；
4. 排除该用户在该域完整序列中出现过的全部正物品，包括后续交互，避免把
   已知正反馈误标成负反馈；
5. 同一条候选列表内负物品不重复；
6. 负样本按 `(user, domain)` 固定缓存。同一用户在同一域的不同上下文复用
   同一负样本列表；训练取前 4 个，验证和测试取前 100 个。

因此，三域任务采用“跨域行为历史 + 目标域内候选排序”：跨域信息用于理解
用户兴趣，但每次排序发生在目标域自己的物品集合中。

### 1.6 验证集与测试集的域间平衡

训练集保留全部可用上下文。验证集和测试集分别对每个域做 seed=2023 的
确定性 reservoir sampling，每个域最多保留 20,000 条，防止大域完全主导
宏平均指标。

| 实验 | 用户数 | 物品数 | 训练上下文 | 验证上下文 | 测试上下文 |
|---|---:|---:|---:|---:|---:|
| 三域 | 456,697 | 208,565 | 3,156,801 | 60,000 | 60,000 |
| 两域 | 290,876 | 116,966 | 1,777,346 | 40,000 | 40,000 |

reservoir sampling 前的评估上下文数量如下：

| 实验 | Split | Beauty | Electronic | Phone |
|---|---|---:|---:|---:|
| 三域 | validation | 185,600 | 202,723 | 52,025 |
| 三域 | test | 206,260 | 185,509 | 47,165 |
| 两域 | validation | — | 201,036 | 50,999 |
| 两域 | test | — | 201,456 | 51,367 |

采样后，每个存在的域在 validation/test 中均为 20,000 条。

### 1.7 防泄漏与冷启动处理

- 目标物品在生成样本后才进入历史；目标评分不作为输入；
- 负样本排除用户在目标域的全部已知正物品；
- 随机用户 ID、物品 ID embedding 只有在训练期出现过时才启用；
- 验证/测试中的冷用户或冷物品，其随机 ID embedding 被置零；
- 冷物品仍可使用冻结的 Qwen 文本向量；
- 数据源文件大小、SHA-256、切分边界和样本数量写入 `metadata.json`。

## 2. ADL 模型结构与算法流程

### 2.1 候选级输入表示

模型为每个“上下文—候选物品”组合构造一条表示：

| 特征 | 维度 | 说明 |
|---|---:|---|
| 用户 ID embedding | 32 | 训练期未见用户置零 |
| 候选物品 ID embedding | 32 | 训练期未见物品置零 |
| 显式域 embedding | 8 | 仅进入预测网络，不进入路由器 |
| 候选物品 Qwen 表示 | 128 | 冻结的 1024 维向量经可训练投影层得到 |
| 全局历史 Qwen 均值 | 128 | 所有历史域共同计算 |
| 同域历史 Qwen 均值 | 128 | 只使用目标域历史 |
| 时间特征 | 8 | 计数、时间间隔和周期特征 |

预测网络输入维度为 `464`。DLM 路由输入排除 8 维显式域 embedding，维度为
`456`，避免路由器直接复制已有域标签，使其有机会发现隐式数据分布。

Qwen 原始向量表保持冻结，1024→128 的投影层参与训练。

### 2.2 Dynamic Learning Module（DLM）

本实验设置 `K=3` 个隐式分布中心。对每个候选级表示 `e_i`：

1. 计算其与每个中心的点积：`s_ik = e_i · c_k`；
2. 对 K 个分数做 softmax，得到软分配系数 `a_ik`；
3. 使用软分配对当前 batch 的表示加权求和并归一化，得到新中心；
4. 上述内部更新重复 3 次；
5. 训练时使用 `beta=0.9` 的 EWMA 更新持久化中心；
6. 取 `argmax(a_ik)` 得到 hard route。

DLM 输入在进入路由前执行 stop-gradient，因此分布中心通过无监督的动态聚类
更新，而不是通过排序标签反向传播。验证和测试阶段冻结中心。

正式实验采用论文 Algorithm 1 的原始点积路由，不对输入表示做 L2
归一化。代码保留 `--normalize-router-input` 作为余弦式消融选项。

路由发生在候选级：同一个用户上下文中的正物品和不同负物品可以被分配到
不同隐式簇。

### 2.3 分布自适应预测网络

预测部分使用五层隐藏 MLP：

```text
464 → 512 → 256 → 128 → 64 → 32 → 1
```

隐藏层后使用 ReLU。每层的有效权重由共享权重和当前隐式簇权重逐元素相乘：

```text
W_effective(k) = W_shared ⊙ W_cluster(k)
```

这样既保留跨分布共享知识，又允许不同隐式簇调整自己的参数。每个候选最终
输出一个 logit。

### 2.4 训练与模型选择

| 参数 | 设置 |
|---|---|
| Optimizer | Adam |
| Learning rate | `1e-3` |
| Batch size | 2,048 个排序上下文 |
| 最大 epoch | 10 |
| Early stopping | patience=2 |
| 模型选择指标 | validation macro NDCG@10 |
| 随机种子 | 2023 |
| 混合精度 | CUDA AMP |

训练把候选第 0 列标为 1，其余负物品标为 0，使用
`BCEWithLogitsLoss`。正样本权重设置为当前负样本数量，即训练时为 4，使一个
上下文内的正负损失总权重大致平衡。DLM 不使用额外监督损失。

### 2.5 评估流程

对每条验证/测试上下文，模型为 101 个候选打分。正物品排名定义为：

```text
rank = 1 + 负物品中分数大于或等于正物品分数的数量
```

随后按域统计 Recall@1/5/10/20/50 和 NDCG@1/5/10/20/50。每条样本只有一个
相关物品，因此命中时 `NDCG@K = 1 / log2(rank + 1)`。模型选择使用各个有
观测域的 NDCG@10 等权平均值，而不是按域样本量加权。

## 3. 实验结果与分析

### 3.1 正式实验结果

三域实验选择 epoch 2，validation macro NDCG@10 为 `0.143498`，test macro
NDCG@10 为 `0.132661`。两域实验选择 epoch 1，validation macro NDCG@10 为
`0.158569`，test macro NDCG@10 为 `0.162943`。

#### Recall

| 实验 | 域 | Recall@1 | Recall@5 | Recall@10 | Recall@20 | Recall@50 |
|---|---|---:|---:|---:|---:|---:|
| 三域 | Beauty | 0.04860 | 0.18630 | 0.29195 | 0.45640 | 0.82245 |
| 三域 | Electronic | 0.04345 | 0.17400 | 0.29650 | 0.48230 | 0.81330 |
| 三域 | Phone | 0.02070 | 0.10620 | 0.22225 | 0.46065 | 0.84910 |
| 两域 | Electronic | 0.05920 | 0.21520 | 0.34935 | 0.53685 | 0.83290 |
| 两域 | Phone | 0.03240 | 0.16235 | 0.31665 | 0.53605 | 0.85840 |

#### NDCG

| 实验 | 域 | NDCG@1 | NDCG@5 | NDCG@10 | NDCG@20 | NDCG@50 |
|---|---|---:|---:|---:|---:|---:|
| 三域 | Beauty | 0.04860 | 0.11772 | 0.15170 | 0.19294 | 0.26518 |
| 三域 | Electronic | 0.04345 | 0.10814 | 0.14743 | 0.19403 | 0.25972 |
| 三域 | Phone | 0.02070 | 0.06191 | 0.09885 | 0.15851 | 0.23593 |
| 两域 | Electronic | 0.05920 | 0.13747 | 0.18053 | 0.22773 | 0.28651 |
| 两域 | Phone | 0.03240 | 0.09589 | 0.14536 | 0.20052 | 0.26473 |

### 3.2 两域与三域结果对比

Electronic 和 Phone 在两域实验中的全部 Recall、NDCG 指标都高于三域实验。
其中变化较明显的是：

- Electronic：Recall@10 从 `0.29650` 提升至 `0.34935`，NDCG@10 从
  `0.14743` 提升至 `0.18053`；
- Phone：Recall@10 从 `0.22225` 提升至 `0.31665`，NDCG@10 从
  `0.09885` 提升至 `0.14536`。

这说明在当前设置下，限制为 Electronic + Phone 更容易获得较好的域内排序
效果，可能与 Beauty 行为带来的跨域噪声或分布差异有关。但不能仅凭这一次
实验断言存在 Beauty 导致的负迁移，原因包括：

1. 当前只有一个随机种子；
2. 两域数据是独立预处理的，用户集合、时间边界、历史和负样本池均发生变化；
3. 不同域的物品规模和交互密度差异较大；
4. 指标来自固定的 101 候选采样任务，而非全库检索。

若要更可靠地判断跨域迁移效果，建议至少补充多个随机种子，并设计保持目标域
测试上下文和负样本完全一致的受控对照实验。

### 3.3 路由消融与簇坍缩

额外运行了 L2 归一化路由表示的消融实验：

| 实验 | 路由方式 | Test macro NDCG@10 | 测试候选 hard-route 数量（K=3） |
|---|---|---:|---|
| 三域 | 原始点积（正式结果） | 0.132661 | 1,193,300 / 1,975,598 / 2,891,102 |
| 三域 | L2 归一化消融 | 0.159077 | 5,433,944 / 0 / 626,056 |
| 两域 | 原始点积（正式结果） | 0.162943 | 1,179,695 / 2,762,788 / 97,517 |
| 两域 | L2 归一化消融 | 0.140647 | 4,036,671 / 0 / 3,329 |

归一化版本在两组实验中都几乎退化为单簇。即使三域归一化版本的 sampled
NDCG@10 更高，它也没有实现 ADL 希望学习多个隐式分布的目标。因此正式结果
采用论文原始点积路由，并同时记录簇使用量，避免只看排序指标而忽略路由退化。

### 3.4 当前实验的主要限制

- 将所有已记录交互都视为正反馈，没有利用评分强弱；
- 负样本为离线采样，没有真实曝光但未点击数据；
- validation/test 每域只保留 20,000 条上下文；
- 同一用户同一域复用固定负样本，便于复现但降低了候选多样性；
- 结果来自单一随机种子；
- sampled-ranking 指标不能直接代表全库线上召回效果。

## 4. 复现位置

- 原始数据：`data/raw/processed_3domains/`
- 三域预处理结果：`data/processed/amazon_3domains/`
- 两域预处理结果：`data/processed/amazon_ele_phone/`
- 三域正式结果：`outputs/amazon_3domains_dot_seed2023/results.json`
- 两域正式结果：`outputs/amazon_ele_phone_dot_seed2023/results.json`
- 汇总结果：`outputs/amazon_adl_metrics.json`、`outputs/amazon_adl_metrics.csv`
- 数据预处理入口：`scripts/prepare_3domains.py`
- 训练入口：`scripts/run_3domain_experiment.py`
